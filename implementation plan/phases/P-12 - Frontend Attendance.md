# P-12: Frontend Attendance

**Owner:** Nigam  
**Goal:** Check-in widget and logs.

## Tasks
1. **Widget:** Build `AttendanceWidget.jsx` in the Navbar. It polls `/attendance/widget` every 30s. Shows Check In / Check Out buttons based on state.
2. **Timer:** Display an auto-incrementing elapsed time when checked in.
3. **List:** `AttendancePage.jsx` to list all records.

**Build Prompt for AI:**
> Implement P-12. Build the live AttendanceWidget for the Navbar with polling and check-in/out logic. Build the Attendance records list view.
