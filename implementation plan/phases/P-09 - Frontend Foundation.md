# P-09: Frontend Foundation

**Owner:** Rohit  
**Goal:** Scaffold the React app.

## Tasks
1. **CSS:** Apply `DESIGN.md` CSS tokens to `src/index.css`.
2. **API Client:** Create `src/api/client.js` wrapping `fetch` with credentials logic.
3. **UI Components:** Build `Badge`, `Button`, `Table`, `SearchInput` in `src/components/ui`.
4. **Layout:** Build `Navbar` (with dropdowns) and `AppLayout`. Wire up React Router in `App.jsx`.

**Build Prompt for AI:**
> Implement P-09. Replace global CSS with design tokens. Build api/client.js. Build base UI components (Button, Badge, Table, Input). Build the Navbar and AppLayout. Setup React Router.
